version: '3.7'

services:
  web:
    build: .
    command: python /books/manage.py runserver 0.0.0.0:8000
    environment:
      - SECRET_KEY=EpR120DQvlu2ZvdfTXFa3Qx6kMQByYyW=EhDo25Dd0sddJozpy4gK7sCgplEeB1Do
      - DEBUG=1
    volumes:
      - .:/books
    ports:
      - "8000:8000"
    depends_on:
      - db

  db:
    image: postgres:15.4
    volumes:
      - postgres_data:/var/lib/postgresql/data/

    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
volumes:
  postgres_data: